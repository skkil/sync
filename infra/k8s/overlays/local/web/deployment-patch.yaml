apiVersion: apps/v1
kind: Deployment
metadata:
  name: web
  namespace: sync
spec:
  template:
    spec:
      containers:
        - name: web
          image: sync-web:local
          imagePullPolicy: Never
